<!DOCTYPE html>
<html>
<head><title>NCAA BRACKET</title>
</head>  
<body>
  <% @brackets.each do |x| %>
  <h5><%= "#{x.name[0..30].gsub(/\s\w+$/,'...')} = #{x.score}" %></h5>
  <% end %>
</body>
</html>    
